c = 1
n = int(input("ingrese un numero "))
while c <= n:
    p = c*2
    print(p)
    c = c+1

input(" ")
# Cuando el usuario ingresa el valor para la variable "n", se generea un ciclo regulado por la variable "c" la cual
# aumentara en 1 con cada repeticion hasta alcanzar el valor de "n", ademas en cada repeticion imprimira la
# variable "p" la cual es el resultado de el numero del ciclo actual multiplicado por 2 lo que dara como resultado
# "n" numero de pares.
# Este codigo utiliza el ciclo "while" por su simplisidad y facilidad de uso ademas se reutilizo el codigo de la
# parte anterior modificando solo algunos pequeños detalles.
